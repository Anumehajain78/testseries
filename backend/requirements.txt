# Phase 2 — contract only. Database, auth and realtime dependencies arrive with
# the steps that need them (step 03 adds sqlalchemy/alembic/psycopg, step 07
# adds redis).
fastapi>=0.141,<1.0
pydantic[email]>=2.13,<3.0
uvicorn[standard]>=0.34
