# Web framework and contract
fastapi>=0.141,<1.0
pydantic[email]>=2.13,<3.0
pydantic-settings>=2.6
uvicorn[standard]>=0.34

# Persistence
sqlalchemy>=2.0,<3.0
alembic>=1.19
psycopg[binary]>=3.2

# Auth
argon2-cffi>=23.0
pyjwt>=2.9

# Realtime
# Cross-worker fan-out for live updates. Optional at runtime: without a
# configured REDIS_URL the API delivers in-process, which is correct for a
# single worker and degrades live updates (never data) beyond one.
redis>=5.0
