# Copy to .env.local. Not committed.

# Where the examination server is. The application does not run without it —
# there is no offline mode, because there is no longer a client-side copy of
# an examination to fall back to.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
